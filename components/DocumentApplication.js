import React from 'react';
import { Modal, Text, View } from 'react-native';

function DocumentApplication({visible=false}) {
    return (
        
            <Modal visible={visible}>
             <Text>dssd</Text>
            </Modal>
        
    );
}

export default DocumentApplication;